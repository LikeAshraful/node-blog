<section class="section main-section">
  <div class="card mb-6">
    <header class="card-header">
      <p class="card-header-title">
        <span class="icon"><i class="mdi mdi-ballot"></i></span>
        Edit Category
      </p>
    </header>
    <div class="card-content">
      <form action="/categories/update/<%= category._id %>" method="post">
        <div class="field">
          <div class="field-body">
            <div class="field">
              <label class="label">Title</label>

              <div class="control">
                <input
                  class="input"
                  type="text"
                  name="title"
                  placeholder="Category Title"
                  value="<%= category.title %>"
                />
              </div>
            </div>
          </div>
        </div>

        <div class="field">
          <label class="label">Description</label>
          <div class="control">
            <textarea
              class="textarea"
              name="description"
              placeholder="Description"
            >
<%= category.description %></textarea
            >
          </div>
        </div>

        <div class="field">
          <label class="label">Is Menu</label>
          <div class="field-body">
            <div class="field">
              <label class="switch">
                <input type="checkbox" value="<%= category.isMenu ? 'true' :
                'false' %>" name="isMenu" <%= category.isMenu ? 'checked' : ''
                %>>
                <span class="check"></span>
                <span class="control-label">Is Menu</span>
              </label>
            </div>
          </div>
        </div>

        <div class="field grouped">
          <div class="control">
            <button type="submit" class="button green">Submit</button>
          </div>
        </div>
      </form>
    </div>
  </div>
</section>
